# DAList internal constructor

Internal function for constructing a DAList object with no/minimal
checks.

## Usage

``` r
new_DAList(x = list())
```

## Arguments

- x:

  An object to be converted into our DAList type

## Value

A DAList object derived from x
