# Sample metadata for Lou HF dataset

Sample metadata for Lou HF dataset

## Usage

``` r
data(Lou_HF_sample_metadata)
```

## Format

A data frame with one row per sample and several metadata columns (e.g.
sample ID, group, batch, and file name).
