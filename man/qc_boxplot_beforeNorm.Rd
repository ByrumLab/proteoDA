% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot_boxplot.R
\name{qc_boxplot_beforeNorm}
\alias{qc_boxplot_beforeNorm}
\title{QC boxplot for data before normalization}
\usage{
qc_boxplot_beforeNorm(
  data,
  groups = NULL,
  sample_labels = NULL,
  pseudo = NULL,
  ...
)
}
\arguments{
\item{data}{A numeric matrix or data.frame of intensities, or a DAList-like
object (list) containing a 'data' matrix.}

\item{groups}{Optional vector indicating class membership. Length must match
the number of samples (columns).}

\item{sample_labels}{Optional character vector of sample labels. Length must
match the number of samples (columns).}

\item{pseudo}{Optional positive numeric value added before log2 transform to
avoid log2(0). If NULL, a small data-driven constant is chosen.}

\item{...}{Additional arguments passed to qc_boxplot.}
}
\value{
A ggplot2 object displaying the QC boxplot of log2-scaled raw data.
}
\description{
Generate a QC boxplot from raw intensity data prior to normalization.
The input may be a numeric matrix or data.frame, or a DAList-like object
containing a top-level element named 'data'. If the data do not appear to
be on the log2 scale, a log2 transform is applied before plotting.
}
\details{
Log2 scale detection uses a conservative heuristic based on data range and
value distribution. When the scale is ambiguous, the function will apply
a log2 transform to ensure comparable distributions.
}
