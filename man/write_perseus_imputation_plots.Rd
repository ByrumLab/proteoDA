% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{write_perseus_imputation_plots}
\alias{write_perseus_imputation_plots}
\title{Write Perseus imputation histograms for each contrast}
\usage{
write_perseus_imputation_plots(
  DAList,
  out_dir = "Imputation_Plots",
  contrasts = NULL,
  samples = NULL,
  bins = 50,
  facet_ncol = 4,
  overlay = TRUE,
  width = 7,
  height = 5,
  dpi = 300,
  device = "png"
)
}
\arguments{
\item{DAList}{A DAList with \code{imputation_per_contrast} diagnostics.}

\item{out_dir}{Directory to save plots (created if missing). If
\code{NULL}, plots are not written to disk.}

\item{contrasts}{Character vector of contrast names to plot; default is
all available contrasts in \code{DAList$imputation_per_contrast}.}

\item{samples}{Optional subset of sample columns to plot (names or
indices); default is all samples.}

\item{bins}{Number of histogram bins.}

\item{facet_ncol}{Number of columns in the facet layout.}

\item{overlay}{Logical; if \code{TRUE}, overlay observed and imputed
intensities in the same panel; if \code{FALSE}, use stacked facets.}

\item{width, height}{Plot size (inches) when saving.}

\item{dpi}{Resolution (dots per inch) when saving.}

\item{device}{Graphics device for \code{ggplot2::ggsave}, e.g.
\code{"png"}.}
}
\value{
A named list of \code{ggplot} objects (returned invisibly).
}
\description{
Uses \code{DAList$imputation_per_contrast[[ct]]$before_log2} and
\code{DAList$imputation_per_contrast[[ct]]$after_log2}
(created by \code{perseus_impute(..., save_before_after = TRUE)})
to generate diagnostic plots for each contrast.
}
\details{
Each plot is a faceted histogram per sample, comparing observed vs imputed
log2 intensities. The helper \code{plot_perseus_imputation()} is used
internally.
}
