% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization_report.R
\name{compute_MD_long}
\alias{compute_MD_long}
\title{Compute MD data for all normalization methods}
\usage{
compute_MD_long(normList, groups)
}
\arguments{
\item{normList}{List of normalized expression matrices (rows = features,
columns = samples), as returned by \code{apply_all_normalizations_contrast()}.}

\item{groups}{Character or factor vector of group labels, length equal to
the number of columns in each matrix in \code{normList}.}
}
\value{
A data.frame with columns \code{A}, \code{M}, \code{method}.
}
\description{
Internal helper: builds a long data.frame with mean intensity (A) and
log2 fold change (M) per feature and normalization method.
}
\details{
For per-contrast mode, \code{groups} is already restricted to the two levels
involved in the contrast. If more than two groups are supplied (global
mode), the first two levels are used with a warning.
}
\keyword{internal}
