% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{normalize_data}
\alias{normalize_data}
\title{Normalize data in a DAList (contrast-aware)}
\usage{
normalize_data(
  DAList,
  norm_method = c("log2", "median", "mean", "vsn", "quantile", "cycloess", "rlr", "gi"),
  input_is_log2 = FALSE,
  contrasts = NULL,
  use_per_contrast_if_available = TRUE,
  ...
)
}
\arguments{
\item{DAList}{A \code{DAList} object.}

\item{norm_method}{One of: "log2","median","mean","vsn","quantile","cycloess","rlr","gi".}

\item{input_is_log2}{Logical. If TRUE, indicates inputs are already on the log2 scale
for methods that expect log2 input. Default FALSE.}

\item{contrasts}{Optional character vector to restrict which contrasts in
\code{DAList$data_per_contrast} are normalized. Default = all available.}

\item{use_per_contrast_if_available}{Logical. Default TRUE. When TRUE and
\code{DAList$data_per_contrast} is present/non-empty, normalize per-contrast and
leave \code{DAList$data} unchanged.}

\item{...}{Additional arguments forwarded to the underlying normalization function.
For example, pass \verb{groups=} to \code{cycloessNorm()} to normalize within groups;
if supplied, \code{groups} will be subset/reordered to the columns present in each
per-contrast matrix.}
}
\value{
The updated \code{DAList}. Tags are updated as follows:
\itemize{
\item Per-contrast path: \code{DAList$tags$per_contrast_normalized = TRUE},
\code{DAList$tags$norm_method_per_contrast} (named list), and
\code{DAList$normalization_per_contrast[[contrast]]$diagnostics} (before/after
per-sample summaries) are recorded. \code{DAList$data} is unchanged.
\item Global path: \code{DAList$tags$normalized = TRUE}, \code{DAList$tags$norm_method = norm_method}.
}
}
\description{
Normalizes data using one of several methods. If \code{DAList$data_per_contrast}
exists and is non-empty, normalization is applied \strong{per-contrast} and the
results are written back to \code{DAList$data_per_contrast[[contrast]]}, leaving
\code{DAList$data} unchanged. Otherwise, it falls back to normalizing
\code{DAList$data} (legacy global behavior).
}
\details{
\strong{Available methods}
\itemize{
\item "log2"       — Binary (base-2) log transformation of raw intensities.
\item "median"     — Divide each sample by its median; rescale by the mean of sample medians.
\item "mean"       — Divide each sample by its mean; rescale by the mean of sample means.
\item "vsn"        — Variance-stabilizing normalization via \code{vsn::justvsn} (expects raw scale).
\item "quantile"   — Quantile normalization via \code{preprocessCore::normalize.quantiles}.
\item "cycloess"   — Cyclic loess via \code{limma::normalizeCyclicLoess} (can be applied within groups).
\item "rlr"        — Robust linear regression (global) normalization.
\item "gi"         — Global intensity normalization (expects raw scale; returns log2).
}

\strong{Log scale behavior}
Methods "median", "mean", "quantile", "cycloess", and "rlr" operate on log2 data.
By default, this function will apply \code{log2} internally before those methods.
If your inputs are already log2-transformed,
set \code{input_is_log2 = TRUE} to avoid double logging.
}
