% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization_report.R
\name{write_norm_eval_report}
\alias{write_norm_eval_report}
\title{Write a post-normalization evaluation report for a single method}
\usage{
write_norm_eval_report(
  DAList,
  norm_label = "cycloess",
  grouping_column,
  output_dir = NULL,
  filename = NULL,
  overwrite = FALSE,
  suppress_zoom_legend = FALSE,
  use_ggrastr = FALSE,
  input_is_log2 = TRUE,
  contrasts = NULL,
  sample_id_col = NULL,
  groups_override = NULL,
  metrics_csv = NULL
)
}
\arguments{
\item{DAList}{A DAList containing already-normalized matrices (per-contrast or global).}

\item{norm_label}{Character scalar used as the list name in plots (e.g., "cycloess").}

\item{grouping_column}{Name of the metadata column giving sample groups. Must contain >= 2 groups.}

\item{output_dir}{Directory to save the PDF (created if missing). Default: working directory.}

\item{filename}{File name for the PDF (must end with \code{.pdf}). Default: "norm_eval_report.pdf".}

\item{overwrite}{Overwrite existing file? Default FALSE.}

\item{suppress_zoom_legend}{Remove legend from the zoomed log2 ratio plot? Default FALSE.}

\item{use_ggrastr}{Use ggrastr in MD plots (if installed) to reduce file size? Default FALSE.}

\item{input_is_log2}{Logical, whether the matrices are already on the log2 scale for metrics
that expect log2 (most are visualization/relative; this flag is unused here but kept for symmetry).}

\item{contrasts}{Optional character vector: restrict to these contrasts when \code{data_per_contrast} is present.}

\item{sample_id_col}{Optional name of the metadata column whose values match matrix colnames.
If \code{NULL}, the function will try to auto-detect a suitable column.}

\item{groups_override}{Optional named vector of group labels with names equal to \strong{matrix column names}.
If supplied, overrides \code{grouping_column}/\code{sample_id_col} matching.}

\item{metrics_csv}{Optional path to a CSV file where per-sample normalization
metrics (e.g., PCV, PMAD, PEV, COR, mean intensity) will be written. If
\code{NULL}, metrics are not exported to CSV.}
}
\value{
Invisibly returns \code{DAList}.
}
\description{
Creates a PDF report with the same panels used in \code{write_norm_report()},
but \strong{using only the already-normalized values} from your \code{DAList}.
This is ideal for evaluating what a chosen normalization (e.g., cyclic loess)
actually did to your data.
}
\details{
If \code{DAList$data_per_contrast} exists, a two-page block is produced \strong{for each contrast}:
metrics grid (PCV, PMAD, PEV, COR, Log2 ratio and zoom) and faceted MD plots.
If no per-contrast data exist, a single two-page report is produced from \code{DAList$data}.
}
