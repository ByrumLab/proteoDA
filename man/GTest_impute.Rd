% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gtest_impute.R
\name{GTest_impute}
\alias{GTest_impute}
\title{G-test for Detection Imbalance and Conditional Imputation}
\usage{
GTest_impute(
  data_matrix,
  group_labels,
  p_threshold = 0.01,
  return_imputed_flags = TRUE
)
}
\arguments{
\item{data_matrix}{A numeric matrix of protein intensities (rows = proteins, columns = samples).}

\item{group_labels}{A character or factor vector specifying the group for each sample (e.g., "N" or "P").}

\item{p_threshold}{A numeric p-value threshold for applying imputation (default = 0.05).}

\item{return_imputed_flags}{Logical; if TRUE, return a logical vector indicating which proteins were imputed.}
}
\value{
A list with:
\describe{
\item{p_values}{Named numeric vector of G-test p-values per protein.}
\item{imputed_data}{Matrix of the same dimension as \code{data_matrix} with imputations applied.}
\item{imputed_flags}{(Optional) Logical vector indicating which proteins were imputed.}
}
}
\description{
Performs a G-test (likelihood ratio test) on per-protein detection counts across two groups
based on observed and expected detection values. Missing values are imputed with the group-wise
minimum if either (1) the p-value is below a defined threshold, or (2) one group has all missing values.
}
