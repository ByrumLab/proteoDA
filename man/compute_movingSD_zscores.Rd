% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_movingSD_zscores.R
\name{compute_movingSD_zscores}
\alias{compute_movingSD_zscores}
\title{Compute Rolling Standard Deviations and Z-scores for logFC Comparisons}
\usage{
compute_movingSD_zscores(results, binsize)
}
\arguments{
\item{results}{An S3 object containing \code{data}, \code{results}, and \code{tags} slots. Must contain:
\itemize{
\item \code{results$data}: A data frame with numeric expression values and a \code{Log2Fold} column.
\item \code{results$results}: A named list of comparisons (e.g., \code{treat_vs_control}) each containing a \code{logFC} vector.
}}

\item{binsize}{Integer. The number of consecutive proteins (rows) to use in each rolling window.}
}
\value{
The input \code{results} object with updated \code{tags}:
\itemize{
\item \code{results$tags$movingSDs}: A list of rolling SD vectors for each comparison.
\item \code{results$tags$logFC_z_scores}: A list of Z-score vectors (logFC divided by rolling SD).
}
}
\description{
This function computes a rolling (moving) standard deviation of log fold change (logFC)
values for each comparison in the \code{results$results} list. The SD is computed over sorted data,
using mean intensity for sorting (without modifying the data). It also computes logFC
Z-scores using the rolling SDs and stores all results in \code{results$tags}.
}
\examples{
results <- compute_moving_sd_and_zscores(results, binsize = 100)

}
