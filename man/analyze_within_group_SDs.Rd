% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SD_within_group.R
\name{analyze_within_group_SDs}
\alias{analyze_within_group_SDs}
\title{Analyze Within-Group Log2FC Standard Deviations}
\usage{
analyze_within_group_SDs(
  count_data,
  sample_metadata,
  group_column = "group",
  output_dir = "QC_dir",
  outlier_method = c("IQR", "z-score"),
  z_thresh = 3
)
}
\arguments{
\item{count_data}{A data frame or matrix of expression data (rows = features, columns = samples).}

\item{sample_metadata}{A data frame with at least two columns: \code{sample} (matching column names of \code{count_data})
and a grouping column (e.g., "group") that specifies sample groupings.}

\item{group_column}{Character string specifying the column name in \code{sample_metadata} used to group samples. Default is \code{"group"}.}

\item{output_dir}{Directory to save plots and summary table. Created if it doesn't exist. Default is \code{"QC_dir"}.}

\item{outlier_method}{Method for identifying SD outliers: \code{"IQR"} (default) or \code{"z-score"}.
\itemize{
\item \code{"IQR"} flags values outside 1.5 × IQR from the 1st and 3rd quartiles.
\item \code{"z-score"} flags values with Z-scores exceeding \code{z_thresh}.
}}

\item{z_thresh}{Z-score threshold for flagging outliers. Only used when \code{outlier_method = "z-score"}. Default is \code{3}.}
}
\value{
Invisibly returns a data frame with per-group statistics:
\item{Group}{Group name}
\item{Mean_SD}{Mean of pairwise log2FC SDs}
\item{SD_of_SDs}{Standard deviation of SDs across log2FC pairs}
\item{N_Pairs}{Number of pairwise comparisons}
\item{Outlier}{Logical flag indicating SD outliers based on the selected method}
}
\description{
Calculates pairwise log2 fold change (log2FC) standard deviations within each sample group,
generates diagnostic density plots, flags groups with outlier SDs, and exports a summary table.
}
\examples{
\dontrun{
  analyze_within_group_SDs(
  my_data$data, 
  my_data$metadata, 
  outlier_method = "z-score",
  z_thresh = 2.5)
}
}
